development:
  adapter: postgresql
  encoding: utf8
  database: ops_hospital_mapper
  host: localhost

staging:
  adapter: postgresql
  encoding: utf8
  database: ops_hospital_mapper
  host: localhost

test: 
  adapter: postgresql
  encoding: utf8
  database: ops_hospital_mapper
  host: localhost
  
production:
  adapter: postgresql
  encoding: utf8
  database: ops_hospital_mapper
  host: localhost
